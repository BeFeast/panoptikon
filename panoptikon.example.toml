listen = "0.0.0.0:8080"
db_path = "./panoptikon.db"

[scanner]
subnets = ["10.10.0.0/24"]
interval_seconds = 60
offline_grace_seconds = 300  # 5 min before marking offline

[auth]
# Password is set on first run via the web UI setup wizard
# session_expiry_seconds = 86400  # 24 hours (default)
